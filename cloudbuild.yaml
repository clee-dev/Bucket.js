steps:
- name: gcr.io/cloud-builders/gcloud
  args: ['compute', 'ssh', '${_USER}@${_INSTANCE}', '--zone', '${_ZONE}', '--command', 'forever stop 0; cd Bucket.js; git pull; npm i; cd src; forever start main.js; exit']
